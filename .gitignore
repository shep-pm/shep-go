# Go build and coverage output. Nothing in this repo is generated into the
# tree except wire.go, which is committed.
*.exe
*.test
*.out
# go build ./... from examples/answers/ names its binary after the
# directory, with no extension on unix.
/examples/answers/answers
